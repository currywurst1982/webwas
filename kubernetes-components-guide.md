# 쿠버네티스 핵심 구성요소 가이드

쿠버네티스를 처음 접하는 사람도 쉽게 이해할 수 있도록, 주요 구성요소를
**아파트 단지 운영**에 비유해 정리한 문서입니다.

---

## 전체 그림

```
            [ Namespace: prod ]  ← 논리적 구역
   ┌──────────────────────────────────────┐
   │  [ Ingress ] → [ Service ]            │
   │                    │                  │
   │              [ Deployment ]           │
   │                    │                  │
   │                 [ Pod ] ──────────┐   │
   │                  │   │            │   │
   │      ConfigMap ──┘   └── Secret    │   │
   │      (일반 설정)       (비밀값)     │   │
   │                                    ▼   │
   │                          [ PVC → PV ]  │
   │                          (영구 저장소) │
   └──────────────────────────────────────┘
```

요청 처리 흐름:

```
 사용자
   │
   ▼
[ Ingress ]      ← URL/경로 보고 라우팅 (정문 안내데스크)
   │
   ▼
[ Service ]      ← 고정 주소 + 부하 분산 (대표 전화번호)
   │
   ▼
[ Deployment ]   ← 팟 개수·버전·복구 관리 (관리 매니저)
   │
   ▼
[ Pod ]          ← 실제 앱이 도는 곳 (방 한 칸)
   │
   ▼
 컨테이너 (앱)
```

---

## 1. 팟 (Pod) — 가장 작은 단위, "방"

- 쿠버네티스가 다루는 **가장 작은 배포 단위**.
- 보통 컨테이너 1개를 담음 (가끔 보조 컨테이너를 함께 담기도 함).
- **수명이 짧음.** 죽으면 고치는 게 아니라 **새 팟이 새로 생성**됨 → IP가 계속 바뀜.
- 비유: 단지 안의 "방 한 칸". 망가지면 새 방을 줌.

## 2. 디플로이먼트 (Deployment) — "방을 관리하는 매니저"

- 팟을 **몇 개, 어떤 버전으로** 띄울지 선언.
- 핵심 기능:
  - **자가 치유**: 팟이 죽으면 자동으로 새로 생성
  - **스케일링**: 개수 증감 (`replicas: 3 → 10`)
  - **롤링 업데이트**: 새 버전을 무중단으로 하나씩 교체
  - **롤백**: 문제 발생 시 이전 버전으로 복귀
- 비유: "항상 방 3개, 인테리어는 v2" 라고 관리하는 매니저.

## 3. 서비스 (Service) — "변하지 않는 대표 전화번호"

- 팟은 IP가 계속 바뀌므로 직접 부르면 안 됨.
- 서비스는 **고정된 이름/IP**를 제공하고 뒤의 팟들로 **부하를 자동 분산**.
- 주요 타입:
  - `ClusterIP` — 클러스터 **내부 통신용** (기본값)
  - `NodePort` — 노드의 특정 포트로 외부 노출
  - `LoadBalancer` — 클라우드 로드밸런서로 외부 노출
- 비유: 매장 대표번호. 직원(팟)이 바뀌어도 같은 번호로 전화하면 누군가 받음.

## 4. 인그레스 (Ingress) — "단지 정문 안내데스크"

- **클러스터 외부(인터넷) 요청을 내부 서비스로 라우팅**하는 입구.
- 주로 HTTP/HTTPS 7계층 라우팅 담당:
  - **경로 기반**: `/api` → A서비스, `/web` → B서비스
  - **호스트 기반**: `api.shop.com` → A, `www.shop.com` → B
  - **TLS/SSL 인증서** 처리 (HTTPS)
- 동작하려면 **인그레스 컨트롤러**(nginx, traefik 등) 설치 필요.
- 비유: 단지 정문 안내데스크. 목적지로 안내.

## 5. ConfigMap — "설정값 메모판"

- 앱의 **일반 설정값을 코드(이미지)와 분리**해 보관.
- 예: DB 주소, 로그 레벨, 기능 on/off 플래그.
- 장점: 설정이 바뀌어도 **이미지 재빌드 불필요**, ConfigMap만 수정.
- 전달 방법: **환경변수** 또는 **파일(볼륨 마운트)**.
- 비유: 단지 게시판의 공용 안내사항.

## 6. Secret — "비밀번호 금고"

- ConfigMap과 유사하지만 **민감한 값**을 담음: 비밀번호, API 키, 인증서.
- 차이점:
  - 값이 **base64로 인코딩**되어 저장 (※ 인코딩일 뿐 암호화 아님 — 별도 설정으로 암호화 가능)
  - 접근 권한(RBAC)을 더 엄격히 제한 가능
- **주의:** base64는 누구나 디코딩 가능 → Secret YAML을 Git에 그대로 올리면 안 됨.
- 비유: 게시판이 아닌 **잠긴 금고**. 허가된 사람만 열람.

> 정리: **일반 설정 → ConfigMap**, **비밀값 → Secret**.

## 7. Namespace — "단지 안의 구역(동) 나누기"

- 하나의 클러스터를 **논리적으로 여러 구역으로 분리**.
- 활용 예: 환경 분리(`dev`/`staging`/`prod`), 팀 분리(`team-a`/`team-b`).
- 효과:
  - **이름 충돌 방지**: 다른 네임스페이스면 같은 이름 사용 가능
  - **권한(RBAC) 분리**: 구역별 접근 제어
  - **리소스 할당량(Quota)**: 구역별 CPU/메모리 상한 지정
- 다른 네임스페이스 호출 시 `서비스명.네임스페이스명` 형식 사용.
- 비유: 같은 단지의 "1동, 2동, 관리동".

## 8. PersistentVolume / PersistentVolumeClaim — "영구 창고"

- 문제: 팟은 죽으면 사라지고 **안의 데이터도 함께 사라짐**.
- 해결: 팟 수명과 **무관하게 유지되는 저장소** → PV/PVC.
- 두 가지 개념:
  - **PV (PersistentVolume)**: 실제 저장 공간 (관리자 준비 또는 자동 생성)
  - **PVC (PersistentVolumeClaim)**: 앱이 내는 저장소 **요청서**
- 팟은 PVC를 통해 저장소를 마운트 → 팟이 재생성돼도 **데이터 보존**.
- `StorageClass`를 쓰면 PVC 요청 시 PV가 **자동 동적 생성**.
- 비유: 방(팟)이 바뀌어도 짐을 보관하는 **공용 창고** + 신청서.

---

## 요약 표

| 구성요소 | 역할 | 한 줄 비유 |
|---|---|---|
| **Pod** | 컨테이너를 담는 최소 실행 단위 | 방 한 칸 |
| **Deployment** | 팟의 개수·버전·복구 관리 | 관리 매니저 |
| **Service** | 팟에 고정 주소 제공 + 부하 분산 | 대표 전화번호 |
| **Ingress** | 외부 요청을 서비스로 라우팅 | 정문 안내데스크 |
| **ConfigMap** | 일반 설정값을 코드와 분리 보관 | 공용 게시판 |
| **Secret** | 비밀번호·키 등 민감 정보 보관 | 잠긴 금고 |
| **Namespace** | 클러스터를 논리적 구역으로 분리 | 단지 안의 1동·2동 |
| **PV / PVC** | 팟과 무관하게 유지되는 영구 저장소 | 공용 창고 + 신청서 |
